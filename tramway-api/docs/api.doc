### Documentation for frontend

## Sign Up

Signing Up is creation of user (of another model, which is user)

Create user

*POST* `/api/v1/records?model=User`

Params structure

```json
{
  data: {
    attributes: {
      # all your attributes in JSON. Attribute name must be in kebab case
    }
  }
}
```
